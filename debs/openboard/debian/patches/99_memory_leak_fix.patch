diff -ruN OpenBoard.orig/src/podcast/ffmpeg/UBFFmpegVideoEncoder.cpp OpenBoard/src/podcast/ffmpeg/UBFFmpegVideoEncoder.cpp
--- a/OpenBoard/src/podcast/ffmpeg/UBFFmpegVideoEncoder.cpp	2019-04-10 10:04:25.000000000 +0300
+++ b/OpenBoard/src/podcast/ffmpeg/UBFFmpegVideoEncoder.cpp	2019-11-19 13:55:15.200526785 +0200
@@ -744,6 +744,7 @@
 {
     AVFrame* frame = mImageQueue.dequeue();
     writeFrame(frame, mVideoPacket, mController->mVideoStream, mController->mOutputFormatContext);
+    av_freep(&frame->data[0]);
     av_frame_free(&frame);
 }
 
@@ -756,6 +757,7 @@
 #if LIBAVFORMAT_VERSION_MICRO < 100
     if (audio_samples_buffer) {
         av_free(audio_samples_buffer);
+        av_freep(&frame->data[0]);
         audio_samples_buffer = NULL;
     }
 #endif
