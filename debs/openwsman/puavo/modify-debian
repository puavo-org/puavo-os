#!/bin/sh

set -eu

sed -i '/^Build-Depends:/s/$/, python3-dev, python3-setuptools/' \
       debian/control

# building with Ruby support is broken
sed -i '/-DBUILD_RUBY_GEM=no/a\
        -DBUILD_RUBY=no' debian/rules
