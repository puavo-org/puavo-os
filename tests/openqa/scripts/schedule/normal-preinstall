#!/bin/bash

set -eu

SCRIPT_DIRECTORY="$(dirname "$0")"
. "$SCRIPT_DIRECTORY/lib/jobs"

qcow2_name='normal-preinstall-%VERSION%-%ARCH%-%BUILD%@%MACHINE%.qcow2'

submit_job normal_preinstall                    \
           boot_diskinstaller,normal_preinstall \
           true                                 \
           "PUBLISH_HDD_1=${qcow2_name}"
