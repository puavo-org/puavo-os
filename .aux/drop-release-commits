#!/bin/bash

set -eu

rebase_file=$1
tmpfile=

trap '[ -n "${tmpfile}" ] && rm -f "${tmpfile}"' EXIT

tmpfile=$(mktemp)

egrep -v '^pick [0-9a-f]{7} RELEASE: .*' "${rebase_file}" >"${tmpfile}"
mv "${tmpfile}" "${rebase_file}"
