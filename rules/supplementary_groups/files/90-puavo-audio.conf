# Desktop users get the audio group from pam_group, not from NSS.
# pam_limits matches @audio via getgrouplist(), so the jackd/pipewire
# @audio and @pipewire rules never apply to Puavo/LDAP users.
# Wildcard limits are not applied to root.
*               -       rtprio          95
*               -       memlock         1024000
