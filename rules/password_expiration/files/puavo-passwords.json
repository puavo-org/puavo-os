{
  "puavo.admin.passwords.expiration.days": {
    "default": "365",
    "description": "Number of days since last user password change before it is considered expired",
    "typehint": "integer"
  },
  "puavo.admin.passwords.expiration.policy": {
    "choices": [ "none", "remind-user" ],
    "default": "none",
    "description": "What to do when user password has expired",
    "typehint": "string"
  }
}
