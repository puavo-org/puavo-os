# disable lock screen option for exammode session
# (it makes no sense to lock the screen if there is no password to open it)

if [ "$USER" = 'puavo-examuser' ]; then
  EXAMMODE_SESSION="$(jq -r .type /var/lib/puavo-exammode/session.json)"
  export EXAMMODE_SESSION
fi
