{
  "puavo.desktop.run_once.scripts": {
    "default": "",
    "description": "List of scripts to run from under /usr/local/share/puavo-run-once-on-desktop-session during login (with the trick that these are run only once per user)",
    "typehint": "string"
  }
}
