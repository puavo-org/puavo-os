{
  "puavo.admin.extra_boot_script.prefixes": {
    "default": "default",
    "description": "A space-separated list of ebs prefixes, it is recommended to always include 'default' in the list",
    "typehint": "string"
  },
  "puavo.admin.run_extra_boot_scripts": {
    "default": "true",
    "description": "Enable/disable extra boot scripts (fetching and executing)",
    "typehint": "bool"
  }
}
