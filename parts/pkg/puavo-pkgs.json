{
  "puavo.pkgs.branch": {
    "default": "stable",
    "description": "The puavo-pkg packages branch to follow",
    "typehint": "string",
    "choices": [ "stable", "devel" ]
  },
  "puavo.pkgs.update": {
    "default": "auto",
    "description": "Puavo-pkg updating policy, automatically (auto), manually (manual) or nightly (nightly)",
    "typehint": "string",
    "choices": [ "auto", "manual", "nightly" ]
  },
  "puavo.pkgs.ui.pkglist": {
    "default": "",
    "description": "A space-separated list of puavo-pkg packages to show in graphical puavo-pkg installer",
    "typehint": "string"
  }
}
