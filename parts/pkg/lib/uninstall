#!/bin/sh

set -eu

package=$1

installdir="/state/restricted-packages/${package}/i"

if [ -x "/usr/share/puavo-ltsp-client/restricted-packages/${package}/prerm" ]; then
    "/usr/share/puavo-ltsp-client/restricted-packages/${package}/prerm" "${installdir}"
fi

rm -rf "${installdir}"

exit 0
