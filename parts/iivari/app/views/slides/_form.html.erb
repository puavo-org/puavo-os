<%= form_for([@channel, @slide], :html => { :multipart => true } ) do |f| %>

<%= render :partial => "template_" + @slide.template, :locals => { :f => f } %>

<%= f.hidden_field :template, :value => @slide.template %>

<%= f.submit %>

<% end %>
