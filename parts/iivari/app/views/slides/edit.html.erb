<% content_for :javascript do %>
  <%= include_tiny_mce_if_needed %>
<% end %>

<h1><%= t('.title') %></h1>

<div class="slide">
<%= render :partial => 'form' %>
</div>

<%= link_to t('links.show'), channel_slide_path(@channel, @slide) %> |
<%= link_to t('links.back'), channel_slides_path %>
