<h1>Listing slides</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @slides.each do |slide| %>
  <tr>
    <td><%= slide.title %></td>
    <td><%= slide.body %></td>
    <td><%= link_to 'Show', channel_slide_path(@channel, slide) %></td>
    <td><%= link_to 'Edit', edit_channel_slide_path(@channel, slide) %></td>
    <td><%= link_to 'Destroy', channel_slide_path(@channel, slide), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Slide', new_channel_slide_path %>
