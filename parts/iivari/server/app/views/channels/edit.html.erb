<h1><%= t('.title') %></h1>

<%= render( :partial => 'form', :locals => { :url => channel_path(@school.puavoId, @channel) } ) %>

<% content_for :tools do %>
<div><%= link_to t('links.show'), @channel %></div>
<% end %>
