(
  set -e
  sleep 10
  # XXX get user and password from some sane place
  env PATH="/opt/puavo-autopilot/bin:$PATH" \
    /opt/puavo-autopilot/bin/login "$(cat /opt/puavo-autopilot/USER)" \
				   "$(cat /opt/puavo-autopilot/PASSWORD)"
) &
