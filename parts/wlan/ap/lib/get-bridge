#!/bin/sh

set -eu

if [ $# -ne 1 ]; then
    echo "$(basename $0): wrong number of arguments ($#)" >&2
    echo "Usage: $(basename $0) SSID" >&2
    exit 1
fi

ssid=$1

echo -n "${ssid}" | md5sum | head -c15
