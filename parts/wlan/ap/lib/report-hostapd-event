#!/bin/sh

set -eu

. puavo-wlanap-env

. "$PWAP_LIBDIR/common.sh"

[ $# -eq 3 ] || {
    pwap_log "wrong number of arguments ($#)"
    echo "Usage: $(basename $0) IFACE EVENT MAC" >&2
    exit 1
}

pwap_report <<EOF
wlaninterface:$1
event:$2
mac:$3
EOF
