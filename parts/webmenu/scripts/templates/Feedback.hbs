<h2>{{ i18n "feedback.title" }}</h2>

<p class="message">
{{ i18n "feedback.how-question" }}
</p>

<div class="mood">
{{#each moods}}
    <img class="{{ cssClass }}" src="{{ imagePath }}" />
{{/each}}
</div>

<div class="question-container textbox">
</div>
