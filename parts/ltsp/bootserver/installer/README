1. Download the latest Ubuntu Precise Pangolin Server 64bit install ISO
   image from http://releases.ubuntu.com/12.04/.

2. Plug in a removable disk (minimum size 1 GiB), here we assume it will
   be /dev/sdb.

2. Run puavo-bootserver-make-installer /dev/sdb /path/to/ubuntu-image.iso
