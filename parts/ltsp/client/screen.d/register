#!/bin/sh

export LANG=fi_FI.UTF-8
export TERM=vt100
setupcon
clear

until /usr/sbin/puavo-register --force --netboot; do true; done

echo
echo -n rebooting...
for i in $(seq 5 | tac); do
  echo -n " $i"
  sleep 1
done

echo
reboot
