#!/bin/sh

if [ "${PUAVO_SESSION_PATH}" = "" ]; then
    CACHE_DIR="/var/lib/puavo-desktop/users/${USER}"
    PUAVO_SESSION_PATH="${CACHE_DIR}/puavo_session.json"

    if [ -f "${PUAVO_SESSION_PATH}" ]; then
      export PUAVO_SESSION_PATH
    fi
fi
