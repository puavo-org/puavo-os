#!/bin/sh

set -eu

# This is a weird test, but we need to distinguish between
# login screen logins and lock screen logins.  On login
# screen our group id is 0(root), on lock screen it is not.
if [ "$(id -g)" -eq 0 ]; then
  # on login screen, must do remove login (getting a kerberos
  # ticket)
  exit 1
fi

# We can do local login.  Renewing our kerberos ticket appears
# to happen in the background and needs no special action.
exit 0
