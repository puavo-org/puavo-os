#!/bin/sh

set -eu

command=$1
shift

case "${command}" in
    download|install|activate|deactivate|uninstall|purge|list)
        "/usr/lib/puavo-ltsp-client/restricted-packages/${command}" "$@"
        ;;
    *)
        echo "E: unknown command '${command}'" >&2
        exit 1
esac

exit 0

