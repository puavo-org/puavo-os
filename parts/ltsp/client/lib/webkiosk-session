#!/bin/sh

set -eu

# Set the environment variable DESKTOP_SESSION to "puavo-webkiosk",
# so we can do necessary tricks in /etc/X11/Xsession.d/38puavo-setup-webkiosk.

exec env DESKTOP_SESSION=puavo-webkiosk \
     /usr/lib/puavo-ltsp-client/desktop-session "$@"
