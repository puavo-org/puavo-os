# Disable all cronjobs (those mostly cause unnecessary load).

rm -f /etc/cron.d/*       \
      /etc/cron.hourly/*  \
      /etc/cron.daily/*   \
      /etc/cron.monthly/* \
      /etc/cron.weekly/*
