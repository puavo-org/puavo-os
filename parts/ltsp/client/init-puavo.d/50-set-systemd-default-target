set_systemd_default_target()
{
    local hosttype

    hosttype=$(puavo-conf puavo.hosttype)

    case "${hosttype}" in
	fatclient|thinclient|laptop)
	    systemctl set-default graphical.target
	    ;;
	*)
	    systemctl set-default multi-user.target
	    ;;
    esac
}

set_systemd_default_target
