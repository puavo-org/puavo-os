#!/bin/sh

SCRIPTSUBDIR=$1

for script in /etc/puavo-vpn-client/scripts/$SCRIPTSUBDIR/*; do
    case "${script}" in
      *.dpkg-new | *.dpkg-dist | *.dpkg-old)
      ;;
    *)
      test -x $script && timeout 30 $script "$@"
      ;;
    esac
done

exit 0
