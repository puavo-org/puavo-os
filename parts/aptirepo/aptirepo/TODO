- transaction log (all imports, updates etc.)

- save imported .changes (not just files listed in .changes) to allow
  easy cross-repository transfers

- add support for importing .deb and .dsc files in addition to .changes
