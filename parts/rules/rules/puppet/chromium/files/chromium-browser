#!/bin/sh

homepage_args() {
  if [ -n "$HOMEPAGE" ]; then
    echo "$HOMEPAGE" \
      | awk -F\| '{ for (i = 1; i <= NF; i++) { print "--homepage", $i } }'
  fi
}

exec /usr/bin/chromium-browser.distrib --no-default-browser-check \
       $(homepage_args) "$@"
