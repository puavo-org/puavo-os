#!/bin/sh

set -eu

rootdir=$1
srcdir=$(dirname $0)

. $srcdir/common.sh

ltsp_scripts="
  $server_scripts
  ltsp/network_interfaces
  ltsp/client
"

setup_buildsrc $srcdir $rootdir
run_scripts $rootdir $ltsp_scripts
