[global]
  # Use the principal name supplied by the server. Makes it possible
  # to always use the same server name in Nautilus. Should be replaced 
  # by some other solution in the future.
  client use spnego principal = yes

