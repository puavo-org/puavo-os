mkdir -p /etc/puavo/wlan

if [ -n "${WLAN_CHANNEL}" ]; then
  echo ${WLAN_CHANNEL} > /etc/puavo/wlan/channel
fi

for I in 01 02 03 04; do
  eval SSID=\$\{WLAN_SSID_${I}\}

  if [ -n "${SSID}" ]; then
    echo $SSID >> /etc/puavo/wlan/ssid
  fi
done
