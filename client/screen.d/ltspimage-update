#!/bin/sh

export LANG=fi_FI.UTF-8
export TERM=vt100
setupcon
clear

ltspimage_name=$(cat /etc/ltsp/this_ltspimage_name)

until /usr/sbin/puavo-install-ltspimages "$ltspimage_name"; do sleep 1; done

echo "Press ENTER to reboot".
read answer
reboot
